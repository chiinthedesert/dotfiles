# Set tmux to use zsh
set -g default-shell /bin/fish

###
# BINDS ------------------------------------------------------
###

unbind C-b
set -g prefix C-space
bind-key C-space send-prefix

# Set new window and pane to have the same path 
unbind '"'
unbind %

bind c new-window -c "#{pane_current_path}"
bind h split-window -c "#{pane_current_path}"
bind v split-window -h -c "#{pane_current_path}"
bind x kill-pane
bind X kill-window

bind-key -n M-1 if-shell "tmux select-window -t :1" "" "new-window -t :1"
bind-key -n M-2 if-shell "tmux select-window -t :2" "" "new-window -t :2"
bind-key -n M-3 if-shell "tmux select-window -t :3" "" "new-window -t :3"
bind-key -n M-4 if-shell "tmux select-window -t :4" "" "new-window -t :4"
bind-key -n M-5 if-shell "tmux select-window -t :5" "" "new-window -t :5"
bind-key -n M-6 if-shell "tmux select-window -t :6" "" "new-window -t :6"
bind-key -n M-7 if-shell "tmux select-window -t :7" "" "new-window -t :7"

bind C command-prompt -p "new session name:" "new-session -d -s '%%' ; switch-client -t '%%'"
bind G command-prompt -p "run command in background session:" 'run-shell "
  target=69;
  while tmux has-session -t \"\$target\" 2>/dev/null; do
    target=\$((target + 1));
  done;
  dir=\"\$(tmux display-message -p \"#{pane_current_path}\")\";
  tmux new-session -d -s \"\$target\" -c \"\$dir\" %1
"'

###
# OPTIONS -------------------------------------------------------
###
# Fix the numbering
set -g base-index 1
set -g pane-base-index 1

set -g mouse on
set -g set-titles on
set -g set-titles-string "#{pane_title}"
set -s focus-events on
set -s extended-keys on
set -s escape-time 0
set -g default-terminal "tmux-256color"

###
#STYLE -----------------------------------------------------------
###
# Position + layout
set -g status-position top
set -g status-justify centre

# Theme zenbones
set -g status-left ' #[fg=#DF69BA,bold]#{s/root//:client_key_table}'
set -g status-right '#[fg=#DF69BA,bold][#S] #[nobold]#(whoami)@#H '
set -g status-style fg='#DF69BA',bg='#4F5B62'

set -g window-status-current-format "#[bold][#I:#W]"
set -g window-status-format "#I:#W"

set -g pane-border-style fg='#DF69BA'
set -g pane-active-border-style fg='#DF69BA'

set -g message-style "fg=#DF69BA,bg=#D3DFE6,bold"

set -g display-panes-active-colour '#DF69BA'
set -g display-panes-colour '#DF69BA'

set -g clock-mode-colour '#DF69BA'
set -g mode-style "reverse"



###
#PLUGIN ----------------------------------------------------------
###
# List of plugins
set -g @plugin 'tmux-plugins/tpm'
set -g @plugin 'tmux-plugins/tmux-sensible'
set -g @plugin 'tmux-plugins/tmux-resurrect'
set -g @plugin 'christoomey/vim-tmux-navigator'
set -g @plugin 'ThePrimeagen/tmux-sessionizer'

# tmux-sessionizer
bind -r f run-shell "tmux neww ~/.tmux/plugins/tmux-sessionizer/tmux-sessionizer"

# Tmux-resurrect 
set -g @resurrect-save 'C-s'
set -g @resurrect-restore 'C-r'
set -g @resurrect-capture-pane-contents 'on'

# vim-tmux-navigator
set -g @vim_navigator_mapping_left "C-Left C-h"
set -g @vim_navigator_mapping_right "C-Right C-l"
set -g @vim_navigator_mapping_up "C-Up C-k"
set -g @vim_navigator_mapping_down "C-Down C-j"
set -g @vim_navigator_mapping_prev ""  # removes the C-\ binding

# Initialize TMUX plugin manager (keep this line at the very bottom of tmux.conf)
run '~/.tmux/plugins/tpm/tpm'
